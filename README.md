# 2DFlowAnalysis
Analysis of a synthetic 2D unsteady flow field to compute important fluid features — vorticity (curl), velocity magnitude, direction field, and particle trajectories — using numerical methods and visualization in Python + OpenGL (C++).
